<UserControl x:Class="StorePOS.Client.Wpf.MVVM.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:StorePOS.Client.Wpf.MVVM.Views"
             xmlns:viewmodels="clr-namespace:StorePOS.Client.Wpf.MVVM.ViewModels"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             Background="{DynamicResource MaterialDesign.Brush.Background}">

    <UserControl.Resources>
        <!-- Side Menu Animation Storyboards -->
        <Storyboard x:Key="ExpandMenuStoryboard">
            <DoubleAnimation Storyboard.TargetName="SideMenuBorder"
                           Storyboard.TargetProperty="Width"
                           To="280"
                           Duration="0:0:0.3">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="MenuLabelsContainer"
                           Storyboard.TargetProperty="Opacity"
                           To="1"
                           Duration="0:0:0.3"
                           BeginTime="0:0:0.1"/>
        </Storyboard>

        <Storyboard x:Key="CollapseMenuStoryboard">
            <DoubleAnimation Storyboard.TargetName="MenuLabelsContainer"
                           Storyboard.TargetProperty="Opacity"
                           To="0"
                           Duration="0:0:0.1"/>
            <DoubleAnimation Storyboard.TargetName="SideMenuBorder"
                           Storyboard.TargetProperty="Width"
                           To="90"
                           Duration="0:0:0.3"
                           BeginTime="0:0:0.1">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>

        <!-- Menu Item Styles -->
        <Style x:Key="MenuItemButtonStyle" TargetType="Button" BasedOn="{StaticResource MaterialDesignFlatButton}">
            <Setter Property="Height" Value="56"/>
            <Setter Property="Margin" Value="8,2"/>
            <Setter Property="Padding" Value="16,0"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Foreground" Value="{DynamicResource MaterialDesign.Brush.Foreground}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="12"
                                Margin="{TemplateBinding Margin}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource MaterialDesign.Brush.Primary.Light}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{DynamicResource MaterialDesign.Brush.Primary}"/>
                                <Setter Property="Foreground" Value="{DynamicResource MaterialDesign.Brush.Primary.Foreground}"/>
                            </Trigger>
                            <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                <Setter Property="Background" Value="{DynamicResource MaterialDesign.Brush.Primary.Light}"/>
                                <Setter Property="Foreground" Value="{DynamicResource MaterialDesign.Brush.Primary}"/>
                            </DataTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Side Menu -->
        <Border x:Name="SideMenuBorder"
                Grid.Column="0" 
                Width="280"
                Background="{DynamicResource MaterialDesign.Brush.Card.Background}"
                BorderBrush="{DynamicResource MaterialDesign.Brush.TextBox.Border}"
                BorderThickness="0,0,1,0">
            
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Header with Hamburger Menu -->
                <Grid Grid.Row="0" Height="64" Margin="16,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Hamburger Menu Button -->
                    <Button x:Name="HamburgerButton"
                            Grid.Column="0"
                            Style="{StaticResource MaterialDesignIconButton}"
                            Width="40" Height="40"
                            Margin="8,0,16,0"
                            Click="HamburgerButton_Click">
                        <materialDesign:PackIcon Kind="Menu" Width="24" Height="24"/>
                    </Button>

                    <!-- Logo/Title -->
                    <StackPanel Grid.Column="1" 
                               x:Name="MenuLabelsContainer"
                               Orientation="Horizontal" 
                               VerticalAlignment="Center">
 
                        <TextBlock Text="Store POS" 
                                  FontSize="20" 
                                  FontWeight="SemiBold"
                                  Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"/>
                    </StackPanel>

                </Grid>

                <!-- Navigation Menu -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Hidden">
                    <StackPanel Margin="0,16">
                        
                        <!-- Dynamic Menu Items -->
                        <ItemsControl ItemsSource="{Binding MenuItems}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Style="{StaticResource MenuItemButtonStyle}" 
                                            ToolTip="{Binding Name}"
                                            Command="{Binding DataContext.NavigateCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding Name}">
                                        <StackPanel Orientation="Horizontal">
                                            <materialDesign:PackIcon Kind="{Binding Icon}" 
                                                                   Width="24" Height="24" 
                                                                   Margin="0,0,16,0"/>
                                            <TextBlock Text="{Binding Name}" VerticalAlignment="Center">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Opacity" Value="{Binding ElementName=MenuLabelsContainer, Path=Opacity}"/>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </StackPanel>
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </ScrollViewer>

                <!-- Footer -->
                <Border Grid.Row="2" 
                       BorderBrush="{DynamicResource MaterialDesign.Brush.TextBox.Border}"
                       BorderThickness="0,1,0,0">
                    <StackPanel>
                        <!-- User Profile -->
                        <Button Style="{StaticResource MenuItemButtonStyle}" ToolTip="Profile">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Account" 
                                                       Width="24" Height="24" 
                                                       Margin="0,0,16,0"/>
                                <TextBlock Text="Profile" VerticalAlignment="Center">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Opacity" Value="{Binding ElementName=MenuLabelsContainer, Path=Opacity}"/>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </StackPanel>
                        </Button>

                        <!-- Logout -->
                        <Button Style="{StaticResource MenuItemButtonStyle}" ToolTip="Logout">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Logout" 
                                                       Width="24" Height="24" 
                                                       Margin="0,0,16,0"/>
                                <TextBlock Text="Logout">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Opacity" Value="{Binding ElementName=MenuLabelsContainer, Path=Opacity}"/>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <!-- Main Content Area -->
        <Grid Grid.Column="1" Background="{DynamicResource MaterialDesign.Brush.Background}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Top Bar -->
            <Border Grid.Row="0" 
                   Background="{DynamicResource MaterialDesign.Brush.Card.Background}"
                   BorderBrush="{DynamicResource MaterialDesign.Brush.TextBox.Border}"
                   BorderThickness="0,0,0,1"
                   Padding="24,16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Page Title -->
                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                        <TextBlock Text="{Binding Title}" 
                                  FontSize="24" 
                                  FontWeight="SemiBold"
                                  Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"/>
                        <TextBlock Text="Welcome back! Here's what's happening in your store today." 
                                  FontSize="14" 
                                  Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"
                                  Opacity="0.7"
                                  Margin="0,4,0,0"/>
                    </StackPanel>

                    <!-- Action Buttons -->
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <Button Style="{StaticResource MaterialDesignIconButton}" 
                                Margin="8,0"
                                Command="{Binding RefreshCommand}"
                                ToolTip="Refresh current view">
                            <materialDesign:PackIcon Kind="Refresh" Width="20" Height="20"/>
                        </Button>
                        <Button Style="{StaticResource MaterialDesignIconButton}" Margin="8,0">
                            <materialDesign:PackIcon Kind="Bell" Width="20" Height="20"/>
                        </Button>
                        <Button Style="{StaticResource MaterialDesignIconButton}" Margin="8,0">
                            <materialDesign:PackIcon Kind="DotsVertical" Width="20" Height="20"/>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Dynamic Content -->
            <ContentPresenter Content="{Binding CurrentView}" Grid.Row="1" Margin="16">
                <!-- Dynamic Content Area -->
            </ContentPresenter>
        </Grid>
    </Grid>
</UserControl>
